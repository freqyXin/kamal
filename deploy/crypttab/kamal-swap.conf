# Candidate crypttab entry for K'amal zram writeback.
# NOT ACTIVE: do not append to /etc/crypttab yet.
#
# No swap option: this mapping must not be formatted with mkswap.
# noauto: activation will be requested by the zram service dependency.
#
# Format: name source key options

kamal-swap /dev/disk/by-backingfile/var-swap /dev/urandom plain,cipher=aes-xts-plain64,size=512,noauto
